<p id="notice"><%= notice %></p>
<div class="homepage-jumbotron">
  <div class="container text-center">

    <div class="row">
      <div class="col-xs-2">

      </div>

      <div class="col-xs-10">
        <nav>
          <ul class="nav navbar-nav">
          </ul>
          <ul class="nav navbar-nav navbar-right">
              <% if !user_signed_in? %>
                  <li><%= link_to "Log In", new_user_session_path %></li>
              <% else %>
                  <li><span class="navbar-text"><%= current_user.email %></span></li>
                  <li><%= link_to "My Bookings", controller: "bookings", action: "user", user_id: current_user %></li>
                  <li><%= link_to "Become a host", new_submarine_path %></li>
                  <li><%= link_to "Log Out", destroy_user_session_path, :method => :delete %></li>
            <% end %>
          </ul>
        </nav>
        <!-- <div class="form-group pull-left">

          <a class="btn btn-lg button-transparent" href="submarines/new" style="color: white">
            Become a Host
          </a>
          <a class="btn btn-lg button-transparent" href="bookings" style="color: white" >
            Log in
          </a>
          <a class="btn btn-lg button-transparent" href="ports/new" style="color: white">
            Add a port
          </a>
          <a class="btn btn-lg button-transparent" href="user_booking/:user_id" style="color: white">
            My bookings
          </a>
        </div> -->
      </div>
    </div>

    <div class="row">
      <div class="col-xs-12">
        <h1 class="homepage-title">Niveau Riche</h1>
        <h3 class="homepage-undertitle">Book submarines from local hosts and experience living in the ocean.</h3>
      </div>
    </div>
  </div>
</div>
<%= form_tag("/ports/test_availability", method: "post") do %>
  Port: <%= select("port", "id", Port.all.collect {|p| [ p.name, p.id ] }, {include_blank: '--select departure port'}, :required => true) %>
  <!-- <%= text_field_tag(:desired_port) %> -->
  Start date: <%= select_date Date.today, prefix: :desired_start_at %>
  <!-- <%= text_field_tag(:desired_start_at) %> -->
  End date: <%= select_date Date.today, prefix: :desired_end_at %>
  <!-- <%= text_field_tag(:desired_end_at) %> -->
  Number of guests: <%= text_field_tag(:desired_num_guests) %>
  <%= submit_tag("Test Search") %>
<% end %>
<div class="row homepage-band center-block">
  <div class="col-xs-12">
    <div class="">
      <form class="form">
        <div class="col-xs-3">
          <input type="text" class="form-control" placeholder="Port">
        </div>
        <div class="col-xs-2">
          <input type="text" class="form-control" placeholder="From">
        </div>
        <div class="col-xs-2">
          <input type="text" class="form-control" placeholder="To">
        </div>
        <div class="col-xs-2">
          <input type="text" class="form-control" placeholder="1 Guest">
        </div>
        <div class="col-xs-2">

          <%= link_to 'Search', {}, {class: "btn button-transparent"} %>

        </div>
      </form>
    </div>
  </div>
</div>
<br>
<div class="container">
  <div class="row">
    <% @ports.each do |port| %>
      <%= link_to(controller: "ports", action: "show", id: port.id) do %>
        <div class="col-md-4 col-sm-6">
          <div class="panel panel-default">
            <%= image_tag(port.image_url, {class: "center-block", style:"width: 100%; height: auto"}) %>
            <%= port.name %>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>
</div>
